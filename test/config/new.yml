defaults: &defaults
  gmail_options:
    :address: 'smtp.gmail.com'
    :port: '587'
    :enable_starttls_auto: true,
  smtp_options:
    :port: '587'
staging:
  <<: *defaults
production:
  <<: *defaults